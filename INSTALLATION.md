# Инструкция по установке LUKSOMER HTML сайта

## 📁 Структура проекта

Убедитесь, что у вас есть следующая структура файлов:

```
Luksomer_html/
├── index.html                  ✅ Главная страница
├── about.html                  ✅ О компании
├── projects.html               ✅ Список проектов
├── project-detail.html         ✅ Детальная страница проекта
├── contacts.html               ✅ Контакты
├── css/
│   └── styles.css             ✅ Все стили сайта
├── js/
│   ├── i18n.js               ✅ Мультиязычность
│   ├── main.js               ✅ Основная логика
│   └── projects.js           ✅ Данные проектов
├── README.md                  ✅ Документация
└── INSTALLATION.md            ✅ Эта инструкция
```

## 🚀 Быстрый старт

### Вариант 1: Локальное открытие

1. Скачайте папку `Luksomer_html`
2. Откройте файл `index.html` в любом браузере
3. Готово! Сайт работает локально

### Вариант 2: Загрузка на хостинг

#### Через FTP (FileZilla, WinSCP)

1. Подключитесь к вашему хостингу через FTP
2. Загрузите ВСЕ файлы из папки `Luksomer_html` в корневую директорию (`public_html` или `www`)
3. Убедитесь, что структура папок сохранена:
   ```
   public_html/
   ├── index.html
   ├── about.html
   ├── projects.html
   ├── project-detail.html
   ├── contacts.html
   ├── css/
   │   └── styles.css
   └── js/
       ├── i18n.js
       ├── main.js
       └── projects.js
   ```
4. Откройте ваш домен в браузере

#### Через панель управления хостингом (cPanel)

1. Войдите в cPanel
2. Найдите "Файловый менеджер" (File Manager)
3. Перейдите в `public_html`
4. Нажмите "Загрузить" (Upload)
5. Загрузите все файлы, сохраняя структуру папок
6. Откройте ваш домен в браузере

## ⚙️ Настройка

### Изменение контактной информации

Откройте любой HTML файл и найдите секцию footer. Замените:

```html
<span>+998 71 123 45 67</span>
<span>info@luksomer.uz</span>
<span>г. Ташкент, ул. Примерная, д. 123</span>
```

На ваши данные.

### Изменение цветовой схемы

Откройте `css/styles.css` и измените переменные в начале файла:

```css
:root {
    --primary-color: #2563eb;     /* Основной цвет */
    --primary-dark: #1d4ed8;      /* Темный оттенок */
    --dark: #0f172a;              /* Цвет фона темных секций */
    --gray-light: #f1f5f9;        /* Цвет фона светлых секций */
}
```

### Добавление/изменение переводов

Откройте `js/i18n.js` и добавьте/измените тексты:

```javascript
const translations = {
    ru: {
        'nav.home': 'Ваш текст',
        // добавьте свои ключи
    },
    uz: {
        'nav.home': 'Sizning matn',
        // добавьте свои ключи
    },
    en: {
        'nav.home': 'Your text',
        // добавьте свои ключи
    }
};
```

Затем используйте в HTML:

```html
<span data-i18n="nav.home">Главная</span>
```

### Замена изображений

Замените URL изображений в HTML файлах на ваши:

```html
<!-- Было -->
<img src="https://images.unsplash.com/photo-..." alt="...">

<!-- Станет -->
<img src="images/ваше-изображение.jpg" alt="...">
```

## 📝 Проверка работоспособности

После загрузки проверьте:

- [ ] Главная страница открывается
- [ ] Все ссылки в меню работают
- [ ] Переключение языков работает
- [ ] Мобильное меню открывается
- [ ] Формы отправляются (показывается alert)
- [ ] Фильтры на странице проектов работают
- [ ] Все изображения загружаются

## 🔧 Требования к хостингу

**Минимальные требования:**
- Поддержка HTML5
- Поддержка JavaScript
- Нет специальных требований к серверу (чистый HTML/CSS/JS)

**Рекомендуемые характеристики:**
- SSL сертификат (HTTPS)
- Gzip сжатие
- Кэширование статических файлов

## 🌐 Настройка домена

1. Укажите DNS записи вашего домена на IP хостинга
2. Дождитесь обновления DNS (до 24-48 часов)
3. Проверьте доступность сайта

## 🔒 Безопасность

**Рекомендации:**

1. **Защита от спама в формах:**
   - Добавьте Google reCAPTCHA
   - Или используйте honeypot поля

2. **HTTPS:**
   - Получите бесплатный SSL от Let's Encrypt
   - Настройте переадресацию HTTP → HTTPS

3. **Резервное копирование:**
   - Настройте автоматическое резервное копирование
   - Храните копии локально

## 📧 Настройка отправки форм

Текущая версия показывает alert при отправке формы. Для реальной отправки:

### Вариант 1: PHP обработчик

Создайте файл `send-form.php`:

```php
<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $name = htmlspecialchars($_POST['name']);
    $email = htmlspecialchars($_POST['email']);
    $phone = htmlspecialchars($_POST['phone']);
    $message = htmlspecialchars($_POST['message']);
    
    $to = "info@luksomer.uz";
    $subject = "Новая заявка с сайта";
    $body = "Имя: $name\nEmail: $email\nТелефон: $phone\nСообщение: $message";
    
    if (mail($to, $subject, $body)) {
        echo json_encode(['success' => true]);
    } else {
        echo json_encode(['success' => false]);
    }
}
?>
```

Измените в `js/main.js`:

```javascript
contactForm.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = new FormData(contactForm);
    
    try {
        const response = await fetch('send-form.php', {
            method: 'POST',
            body: formData
        });
        
        const result = await response.json();
        
        if (result.success) {
            alert('Спасибо! Ваша заявка отправлена.');
            contactForm.reset();
        }
    } catch (error) {
        alert('Ошибка отправки. Попробуйте позже.');
    }
});
```

### Вариант 2: Сторонние сервисы

- **Formspree**: https://formspree.io
- **EmailJS**: https://www.emailjs.com
- **Getform**: https://getform.io

## 🎨 Кастомизация

### Добавление новых проектов

1. Откройте `projects.html`
2. Скопируйте блок проекта:

```html
<a href="project-detail.html?id=7" class="project-card" data-category="lines">
    <div class="project-image">
        <img src="images/project-7.jpg" alt="Project 7">
    </div>
    <div class="project-content">
        <div class="project-year">2024</div>
        <h3 class="project-title">Название проекта</h3>
        <p class="project-description">Описание проекта</p>
    </div>
</a>
```

3. Добавьте данные в `js/projects.js`

### Добавление новых страниц

1. Скопируйте `about.html` как шаблон
2. Измените контент
3. Добавьте ссылку в меню навигации

## 💡 Советы по оптимизации

1. **Сжатие изображений:**
   - Используйте TinyPNG или Squoosh
   - Рекомендуемый размер: до 200 KB на изображение

2. **Минификация:**
   - CSS: используйте cssnano или clean-css
   - JS: используйте UglifyJS или Terser

3. **CDN:**
   - Рассмотрите использование Cloudflare
   - Ускорит загрузку сайта

## 🐛 Решение проблем

### Сайт не открывается

1. Проверьте, что файлы загружены в правильную папку
2. Убедитесь, что файл называется `index.html` (со строчной буквы)
3. Проверьте права доступа к файлам (должны быть 644)

### Не работают стили

1. Проверьте путь к CSS: `<link rel="stylesheet" href="css/styles.css">`
2. Убедитесь, что папка `css` находится в той же директории, что и HTML файлы
3. Проверьте консоль браузера (F12) на ошибки

### Не работает переключение языков

1. Проверьте, что файл `js/i18n.js` загружен
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что порядок подключения скриптов правильный:
   ```html
   <script src="js/i18n.js"></script>
   <script src="js/main.js"></script>
   ```

### Не работает мобильное меню

1. Проверьте, что `js/main.js` загружен
2. Проверьте консоль браузера (F12)
3. Попробуйте открыть в режиме инкогнито

## 📞 Поддержка

Если возникли вопросы:

1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Network вкладку на наличие ошибок загрузки
3. Убедитесь, что все файлы загружены на хостинг
4. Проверьте, что структура папок соблюдена

## ✅ Финальный чеклист

Перед запуском убедитесь:

- [ ] Все файлы загружены на хостинг
- [ ] Структура папок сохранена
- [ ] Права доступа к файлам установлены (644 для файлов, 755 для папок)
- [ ] Домен настроен и работает
- [ ] SSL сертификат установлен (HTTPS)
- [ ] Контактные данные обновлены
- [ ] Все ссылки работают
- [ ] Формы тестируются
- [ ] Сайт открывается на мобильных устройствах
- [ ] Все изображения загружаются
- [ ] Переключение языков работает

---

**Готово!** Ваш сайт готов к работе! 🎉
